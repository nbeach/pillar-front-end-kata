{
  "private": true,
  "scripts": {
    "build": "gulp",
    "build:watch": "gulp watch & reload --dir dist/",
    "test": "echo 'No tests exist for this project'",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-cssmin": "^0.2.0",
    "gulp-less": "^3.3.2",
    "gulp-pug": "^3.3.0",
    "gulp-rename": "^1.2.2",
    "gulp-sourcemaps": "^2.6.0",
    "reload": "^2.0.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.7"
  }
}
